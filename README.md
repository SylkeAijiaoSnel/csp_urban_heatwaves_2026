# Complex-Systems-2026
# csp_urban_heatwaves_2026
Group project

We applied the SIP/D2D protocol to translate the causal loop diagram into a semi-quantitative dynamic representation. Variables were classified as stocks, auxiliaries, or constants, and as fast- or slow-changing variables relative to a time horizon of .... years. 

The analysis suggests that loop [R1/B1: naam] is dominant during [fase/periode]. As [variabele A] increases, [variabele B] changes in the same/opposite direction, which ultimately [versterkt/dempt] the initial change in [variabele A]. This produces [beschrijf patroon in de simulatie].

Later in the simulation, loop [R2/B2: naam] becomes more influential because [verklaring op basis van je CLD en D2D-output]. This indicates that policy interventions should target [variabele of relatie], because it affects the dominant mechanism rather than only treating a symptom.
